# grimuah rule candidates from the sleepy research corpus

generated by docs/rule-candidates/harness.mjs, verified rules: 31, corpus: 129 files, holdout: 28 files

## architecture

### config-declares-data-only

- layer: structural, tier: grit, severity: warn
- rule: a .config.ts file must declare data only, never a function
- why: a config file sits at the lowest dagOrder of its surface, so an exported function there is callable from every layer above it with no firewall rule describing the dependency, the file stops being a declaration and becomes a module of behaviour that bypasses the surface that owns it, and because the function usually needs types or constants from the surface, it pulls the config into an import cycle the graph cannot express
- replacement: move the function into the surface's own module, or into lib when several surfaces need it, and keep the config file exporting enums and constants only
- hits: 4 across 3 files (holdout 0)

evidence:

```
  src/services/divination.service.config.ts:13  currentDateKey = (): string => new Date().toISOString().slice(0, 10) -- config files declare data, this is behaviour
  src/services/dream-journey.service.config.ts:444  visitTierForCount = ( completedJourneyCount: number, ): DreamJourneyVisitTier => { if (completedJourneyCount = -- config files declare data, this is behaviour
  src/services/spirit-chase.service.config.ts:67  parseSpiritChaseTier = ( rawTier: string, ): SpiritChaseTier | undefined => SpiritChaseTierOrder.find((tier) = -- config files declare data, this is behaviour
  src/services/spirit-chase.service.config.ts:72  rollSpiritChaseTier = (): SpiritChaseTier => { const rollPercent = Math.random() * 100; let cumulativeWeightPe -- config files declare data, this is behaviour
```

### no-export-without-consumer

- layer: structural, tier: prepass, severity: warn
- rule: an exported binding must be named by at least one other module
- why: the export keyword widens a symbol's audience to the whole project, so a reader who finds one has to assume something outside can depend on its shape. when no other module ever names the symbol the promise is false, the declaration is really module private, and every future edit carries a compatibility question the project cannot answer. this is a different defect from a declaration nothing uses at all, the symbol here is live inside its own module and only the visibility is wrong
- replacement: drop the export keyword when only this module names the symbol. keep the export if another module is meant to, then have that module name it, which is what makes the contract visible. top level root modules (the shared library and process entry points) are out of scope, their export list is an api no single application can prove unconsumed
- hits: 168 across 35 files (holdout 0)

evidence:

```
  src/db/accounts.repo.ts:8  TypeAliasDeclaration GetAccountBalanceOutcome is exported but no other module names it
  src/db/accounts.repo.ts:10  TypeAliasDeclaration UpdateAccountBalanceOutcome is exported but no other module names it
  src/db/catches.repo.ts:3  EnumDeclaration CatchFailureReason is exported but no other module names it
  src/db/catches.repo.ts:7  TypeAliasDeclaration ListCatchStacksOutcome is exported but no other module names it
  src/db/consumables.repo.ts:10  TypeAliasDeclaration ConsumablePurchaseOutcome is exported but no other module names it
  src/db/consumables.repo.ts:12  TypeAliasDeclaration ConsumeConsumableOutcome is exported but no other module names it
  src/db/dream-journeys.repo.ts:3  EnumDeclaration DreamJourneyFailureReason is exported but no other module names it
  src/db/dream-journeys.repo.ts:7  TypeAliasDeclaration GetCompletedJourneyCountOutcome is exported but no other module names it
```

### no-import-cycles

- layer: structural, tier: prepass, severity: error
- rule: files must not form an import cycle, even within one surface
- why: the import firewall keeps imports flowing from deep surfaces to shallow ones, but two files at the same dagOrder can still import each other, and a cycle makes module initialisation order load-bearing, hides the true dependency direction, and lets a value read at module scope be undefined depending on which file the runtime reached first
- replacement: lift the shared symbols into a third module at or above the shallower of the two, or invert one direction with a callback or a parameter so the dependency points one way
- hits: 5 across 5 files (holdout 0)

evidence:

```
  gateway/music-bot.ts:114  ./music-messages.ts -- imports back into a 2 file cycle: gateway/music-messages.ts,gateway/music-bot.ts
  gateway/music-messages.ts:18  ./music-bot.ts -- imports back into a 2 file cycle: gateway/music-messages.ts,gateway/music-bot.ts
  src/services/family.service.config.ts:1  ./family.service.ts -- imports back into a 3 file cycle: src/services/family.types.ts,src/services/family.service.ts,src/services/family.service.config.ts
  src/services/family.service.ts:7  ./family.service.config.ts -- imports back into a 3 file cycle: src/services/family.types.ts,src/services/family.service.ts,src/services/family.service.config.ts
  src/services/family.types.ts:1  ./family.service.config.ts -- imports back into a 3 file cycle: src/services/family.types.ts,src/services/family.service.ts,src/services/family.service.config.ts
```

### no-redundant-allowed-import

- layer: structural, tier: prepass, severity: warn
- rule: an allowedImports entry naming a shallower surface is never consulted and must be removed
- why: the import firewall already permits every import from a deeper surface to a shallower one, so an allowlist entry naming a shallower surface is dead configuration that the checker never reads, a reader cannot tell which entries are load bearing and which merely restate the dag order, and the whole allowlist stops adding up to anything once its entries are indistinguishable from the default
- replacement: delete the entry and keep only the grants that permit a real backward edge, a deeper surface listed for a shallower one. in grimuah this finding belongs on architecture.config.json rather than on a source file
- hits: 13 across 5 files (holdout 42)

evidence:

```
  src/commands/afk.command.config.ts:1  surface 'commands' (dagOrder 5) grants 'lib' (dagOrder 0) -- the dag already permits this, the entry is never read
  src/commands/afk.command.config.ts:1  surface 'commands' (dagOrder 5) grants 'db' (dagOrder 1) -- the dag already permits this, the entry is never read
  src/db/accounts.repo.ts:1  surface 'db' (dagOrder 1) grants 'lib' (dagOrder 0) -- the dag already permits this, the entry is never read
  src/handlers/interaction.handler.config.ts:1  surface 'handlers' (dagOrder 7) grants 'db' (dagOrder 1) -- the dag already permits this, the entry is never read
  src/handlers/interaction.handler.config.ts:1  surface 'handlers' (dagOrder 7) grants 'services' (dagOrder 2) -- the dag already permits this, the entry is never read
  src/middleware/signature-verification.middleware.config.ts:1  surface 'middleware' (dagOrder 3) grants 'lib' (dagOrder 0) -- the dag already permits this, the entry is never read
  src/middleware/signature-verification.middleware.config.ts:1  surface 'middleware' (dagOrder 3) grants 'db' (dagOrder 1) -- the dag already permits this, the entry is never read
  src/services/afk.service.config.ts:1  surface 'services' (dagOrder 2) grants 'lib' (dagOrder 0) -- the dag already permits this, the entry is never read
  src/services/afk.service.config.ts:1  surface 'services' (dagOrder 2) grants 'db' (dagOrder 1) -- the dag already permits this, the entry is never read
```

### require-enum-in-config-file

- layer: structural, tier: prepass, severity: warn
- rule: an exported enum must be declared in its surface's .config.ts file
- why: an enum is a configuration constant, and grimuah gives configuration its own innate member so its dag order is declared rather than inherited from whatever module happened to declare it, when the enum lives in the repository or service that raises it every consumer imports an implementation module to read a constant, and the declaration drifts into an import cycle because the module that owns the behaviour ends up owning the vocabulary too
- replacement: move the enum into the surface's .config.ts file, creating it when the surface has none, so the constant has a declaration home and the implementing module exports behaviour only
- hits: 29 across 25 files (holdout 0)

evidence:

```
  src/db/accounts.repo.ts:3  AccountFailureReason is declared in an implementation module -- move it to accounts.config.ts
  src/db/catches.repo.ts:3  CatchFailureReason is declared in an implementation module -- move it to catches.config.ts
  src/db/consumables.repo.ts:5  ConsumableFailureReason is declared in an implementation module -- move it to consumables.config.ts
  src/db/dream-journeys.repo.ts:3  DreamJourneyFailureReason is declared in an implementation module -- move it to dream-journeys.config.ts
  src/db/liked-tracks.repo.ts:3  LikedTracksFailureReason is declared in an implementation module -- move it to liked-tracks.config.ts
  src/db/nightmare-jar.repo.ts:3  NightmareJarFailureReason is declared in an implementation module -- move it to nightmare-jar.config.ts
  src/db/shop.repo.ts:5  ShopPurchaseFailureReason is declared in an implementation module -- move it to shop.config.ts
  src/db/spirit-chase.repo.ts:9  SpiritChaseFailureReason is declared in an implementation module -- move it to spirit-chase.config.ts
```

### require-shared-type-placement

- layer: structural, tier: prepass, severity: warn
- rule: a type consumed from another directory must live in a declaration file, not an implementation module
- why: when the shared type sits inside the module that also implements behaviour, every consumer of the type imports the implementation, the dependency the import firewall reasons about is no longer the one the reader sees, the consumer is coupled to that module's other exports, and the type and the behaviour can no longer change independently
- replacement: declaring the type in the surface's .types.ts file gives the shared shape its own module, keeps the import edge pointing at a declaration, and lets the implementing module keep its behaviour private. enums are handled by require-enum-in-config-file instead, so the two rules never offer the same fix twice
- hits: 6 across 3 files (holdout 0)

evidence:

```
  src/db/liked-tracks.repo.ts:20  LikedTrack is imported from another directory -- declare it in liked-tracks.types.ts
  src/db/spirit-chase.repo.ts:15  SpiritChaseRecord is imported from another directory -- declare it in spirit-chase.types.ts
  src/db/spirit-chase.repo.ts:39  SpiritChaseInsert is imported from another directory -- declare it in spirit-chase.types.ts
  src/services/music.service.ts:80  MusicEnvelope is imported from another directory -- declare it in music.types.ts
  src/services/music.service.ts:104  MusicControlRequest is imported from another directory -- declare it in music.types.ts
```

## complexity

### max-cyclomatic-complexity

- layer: resilience, tier: grit, severity: warn
- rule: a function must not exceed cyclomatic complexity 15
- why: cyclomatic complexity counts the independent paths a reader must hold in their head at once, a function past the threshold is one where each new branch silently doubles the states to reason about, tests stop covering the branches and start covering the happy path, and the function is usually several decisions that were never separated
- replacement: extract each decision into a named predicate or lookup, replace branch chains with a Record dispatch table, and move the removed cases into their own functions with their own names
- hits: 7 across 6 files (holdout 0)

evidence:

```
  gateway/music-bot.ts:892  complexity 29 -- private async dispatchCommand( guild: Guild, command: ControlCommand, ): Promise<ControlEn
  gateway/music-bot.ts:1160  complexity 24 -- (rawBody: unknown): ControlCommand | undefined => { if (!isRecord(rawBody)) { return undef
  src/commands/jar.command.ts:80  complexity 17 -- async ( interaction: APIChatInputApplicationCommandInteraction, env: CommandEnv, _context,
  src/commands/music.command.ts:374  complexity 22 -- async (interaction, env, context): Promise<Response> => { const guildId = interaction.guil
  src/commands/sleep.command.ts:418  complexity 18 -- async (interaction, env, _context): Promise<Response> => { const guildId = interaction.gui
  src/services/music.service.ts:175  complexity 24 -- ( rawData: Record<string, unknown>, ): MusicEnvelopeData | undefined => { const { kind: ra
  src/services/sleep.service.ts:369  complexity 39 -- async ( dreamsDb: D1Database, botToken: string, record: NightmareRecord, creepyLine: strin
```

### max-file-lines

- layer: resilience, tier: grit, severity: warn
- rule: a module must not exceed 500 lines
- why: a module past the cap has almost always grown a second responsibility because the first one no longer fits the file's name, every reader pays the whole file's cost to find one function, and the file becomes the place changes are made by default rather than the place one concern is expressed
- replacement: split the module along the responsibilities already visible in its section comments, giving each extracted part a name that says what it does and an import edge that shows who needs it
- hits: 12 across 12 files (holdout 0)

evidence:

```
  gateway/music-bot.ts:1  1323 lines in gateway/music-bot.ts, over the 500 line cap
  src/commands/sleep.command.ts:1  570 lines in src/commands/sleep.command.ts, over the 500 line cap
  src/services/dream-journey.service.ts:1  860 lines in src/services/dream-journey.service.ts, over the 500 line cap
  src/services/family-consent.service.ts:1  640 lines in src/services/family-consent.service.ts, over the 500 line cap
  src/services/family.service.ts:1  971 lines in src/services/family.service.ts, over the 500 line cap
  src/services/guide-shelf.service.config.ts:1  718 lines in src/services/guide-shelf.service.config.ts, over the 500 line cap
  src/services/guide-shelf.service.ts:1  901 lines in src/services/guide-shelf.service.ts, over the 500 line cap
  src/services/music.service.ts:1  509 lines in src/services/music.service.ts, over the 500 line cap
```

### max-function-lines

- layer: resilience, tier: grit, severity: warn
- rule: a function body must not exceed 80 lines
- why: a long function accumulates responsibilities because every addition looks local, the name stops describing the whole body, and the only way to test one branch is to reproduce the state the earlier branches built, length is the cheapest proxy for how many jobs a function is doing
- replacement: extract each distinct job into a named function with its own return type and leave the original as an ordered sequence of calls
- hits: 20 across 15 files (holdout 0)

evidence:

```
  gateway/music-bot.smoke.ts:90  89 lines -- async (): Promise<void> => { console.log("resolving a free-text query through ytsearch1");
  gateway/music-bot.ts:892  166 lines -- private async dispatchCommand( guild: Guild, command: ControlCommand, ): Promise<ControlEn
  src/commands/afk.command.ts:78  87 lines -- async (interaction, env, context): Promise<Response> => { const guildId = interaction.guil
  src/commands/divination.command.ts:106  86 lines -- async (interaction, env, context): Promise<Response> => { const guildId = interaction.guil
  src/commands/dream-fishing.command.ts:85  116 lines -- async (interaction, env, _context): Promise<Response> => { const guildId = interaction.gui
  src/commands/jar.command.ts:80  98 lines -- async ( interaction: APIChatInputApplicationCommandInteraction, env: CommandEnv, _context,
  src/commands/level.command.ts:93  142 lines -- async (interaction, env, _context): Promise<Response> => { const guildId = interaction.gui
  src/commands/music.command.ts:374  94 lines -- async (interaction, env, context): Promise<Response> => { const guildId = interaction.guil
```

### max-parameters

- layer: resilience, tier: grit, severity: warn
- rule: a function must not take more than 4 parameters
- why: each parameter is a position the caller must fill in the right order, so a long parameter list makes every call site a puzzle and every reordering a silent bug, it is also the shape that appears when a function has grown to need data from several layers instead of receiving one cohesive value
- replacement: group the parameters into a named readonly type and pass one object, or split the function until each part needs fewer inputs
- hits: 45 across 19 files (holdout 0)

evidence:

```
  src/commands/dream-fishing.command.ts:41  6 parameters -- async ( env: CommandEnv, guildId: string | undefined, userId: string, readyAt: number, des
  src/commands/level.command.ts:42  5 parameters -- async ( env: CommandEnv, guildId: string, userId: string, level: number, heldRoleIds: read
  src/commands/music.command.ts:123  5 parameters -- ( startedPlayback: boolean, addedCount: number, firstTrackTitle: string, firstTrackUrl: st
  src/commands/sleep.command.ts:138  5 parameters -- ( creepyLine: string, earnedDreams: number, effects: readonly ConsumedEffect[], omenNotice
  src/commands/sleep.command.ts:181  6 parameters -- async ( env: CommandEnv, guildId: string | undefined, userId: string, nextReadyAt: number,
  src/db/accounts.repo.ts:74  5 parameters -- async ( dreamsDb: D1Database, userId: string, baseDreams: number, perkItemId: string, perk
  src/db/shop.repo.ts:74  5 parameters -- async ( dreamsDb: D1Database, userId: string, itemId: string, price: number, purchasedAt: 
  src/db/spirit-chase.repo.ts:153  5 parameters -- async ( dreamsDb: D1Database, chaseId: string, clickedMessageId: string, catcherUserId: st
  src/db/spirit-chase.repo.ts:232  5 parameters -- async ( dreamsDb: D1Database, chaseId: string, newChannelId: string, newMessageId: string,
```

### no-nested-ternary

- layer: resilience, tier: grit, severity: warn
- rule: a conditional expression must not contain another conditional expression
- why: a ternary nested in a ternary turns a two-branch decision into a tree the reader must evaluate branch by branch, the conditions read in one direction and the results in another, and the shape is exactly the case a lookup table or an early return expresses directly
- replacement: extract the inner decision into a named helper, look the branch up in a Record, or replace the chain with early returns when it chooses between statements
- hits: 18 across 4 files (holdout 0)

evidence:

```
  src/commands/music.command.ts:133  startedPlayback ? MusicReplyMessage.PlayedSingleTemplate : MusicReplyMessage.QueuedSingleTemplate
  src/commands/music.command.ts:136  startedPlayback ? MusicReplyMessage.PlayedPlaylistTemplate : MusicReplyMessage.QueuedPlaylistTemplate
  src/services/dream-journey.service.ts:315  chapter1Stance === chapter3Stance ? chapter1Stance : chapter2Stance === chapter3Stance ? chapter2Stance : undefined
  src/services/dream-journey.service.ts:317  chapter2Stance === chapter3Stance ? chapter2Stance : undefined
  src/services/music.service.ts:483  customId === "music:skip" ? { command: "skip", guildId, userId: memberUserId } : customId === "music:stop" ? { command: 
  src/services/music.service.ts:485  customId === "music:stop" ? { command: "stop", guildId, userId: memberUserId } : customId === "music:volume-up" ? { comm
  src/services/sleep.service.ts:473  jarredCatcherDoubled ? JarredCatcherTheftLines : JarredTheftRevealLines
```

## declarative

### no-for-of-push-accumulation

- layer: resilience, tier: grit, severity: error
- rule: a for..of loop must not build an array by pushing into it
- why: a loop that pushes into an array is a map, a filter or a reduce written the long way, the reader has to run the loop in their head to learn what the result contains, the accumulator is a mutable binding the rule set otherwise forbids, and the loop body hides whether elements are kept, dropped or transformed
- replacement: express the transformation with map, filter, flatMap or reduce, and when the body is genuinely side-effecting, keep the for..of but drop the accumulator
- hits: 6 across 6 files (holdout 0)

evidence:

```
  src/services/family-consent.service.ts:582  for (const targetId of activeTargetIds) { const record = (await familyKv.get<ConsentRecord>( buildConsentKey(targetId), 
  src/services/family-tree.service.ts:161  for (const parentId of childEntry.record.parents) { const parentBoxX = parentBoxXByUserId.get(parentId); if (parentBoxX 
  src/services/family.service.ts:235  for (const key of keyList.keys) { const record = await familyKv.get<FamilyRecord>(key.name, "json"); if (record && recor
  src/services/pillow-fight.service.ts:933  for (const actorId of activeActorIds) { const record = (await afkKv.get<PillowFightRecord>( buildPillowFightKey(actorId)
  src/services/sleepover.service.ts:793  for (const targetId of activeTargetIds) { const record = (await sleepKv.get<SleepoverRecord>( buildSleepoverKey(targetId
  src/services/spirit-chase.service.ts:1497  for (const chaseRecord of expiredOutcome.result) { const messageDissolved = await dissolveChaseMessage( botToken, chaseR
```

### no-if-chain-dispatch

- layer: resilience, tier: grit, severity: warn
- rule: a run of three or more branches over one subject is a dispatch table
- why: the shipped switch ban states the intent as use a dispatch table, and an if-chain of predicate tests and early returns is the same construct with the same properties: one linear scan, one function to edit to add a case, and no place that lists the cases. it also passes the ban. the run is recognisable without types because every branch tests the same subject and every branch returns, which is what separates a dispatch from a run of unrelated guards
- replacement: declare a Record or Map from the subject's value to the handler and look the branch up, so adding a case is adding an entry and the set of cases is a value a reader can see. keep the if-chain when the branches test genuinely different conditions rather than different values of one subject
- hits: 3 across 3 files (holdout 0)

evidence:

```
  gateway/member-join-bridge.ts:316  6 branches dispatched on action.kind
  src/handlers/interaction.handler.ts:121  9 branches dispatched on interaction.data.custom_id
  src/services/shop.service.ts:255  3 branches dispatched on item.kind
```

## patterns

### no-boolean-flag-argument

- layer: resilience, tier: grit, severity: warn
- rule: a call must not pass a bare boolean literal as an argument
- why: a boolean argument makes one function do two jobs and hides which job the call site asked for, true and false read identically at the call, so the reader has to open the callee to learn what the position means, and every new variant adds another positional flag that only the callee can decode
- replacement: give the two behaviours their own named functions, or pass a named enum or union value so the call site says what it wants, and keep a boolean parameter only when the callee is a third-party api whose signature you do not control
- hits: 21 across 4 files (holdout 0)

evidence:

```
  src/services/family-consent.service.ts:271  buildConsentButtonRow(false)
  src/services/family-consent.service.ts:441  buildConsentButtonRow(true)
  src/services/family.service.ts:302  patchMemberMarriedRole(true)
  src/services/family.service.ts:328  patchMemberMarriedRole(false)
  src/services/pillow-fight.service.ts:451  buildPillowFightDecisionRow(false)
  src/services/pillow-fight.service.ts:520  buildPillowFightDecisionRow(true)
  src/services/sleepover.service.ts:303  buildSleepoverButtonRow(false)
  src/services/sleepover.service.ts:505  buildSleepoverButtonRow(true)
```

### no-duplicated-computation

- layer: resilience, tier: prepass, severity: warn
- rule: a computation written twice in two files must be extracted into a shared function
- why: an identical expression in two modules is one decision written twice: a change to the unit, the rounding or the route has to be found and applied in both copies, and the copies drift one bug report at a time because nothing marks them as related. the compiler sees two independent expressions, so does the reader, and the second copy is invisible to any search that looks for a call by name. the repo mandate states it plainly: do not write the same derived computation twice, extract it.
- replacement: extract the expression into a named function in the shared module at the bottom of the dependency order and call it from both places, so the formula has one home and one name.
- hits: 26 across 20 files (holdout 0)

evidence:

```
  gateway/music-messages.ts:37  Math.floor(totalSeconds / secondsPerMinute) -- the same computation is written in src/services/music.service.ts
  gateway/music-messages.ts:38  Math.floor(totalSeconds % secondsPerMinute) -- the same computation is written in src/services/music.service.ts
  gateway/register-commands.ts:429  new REST({ version: "10" }).setToken(botToken) -- the same computation is written in lib/discord-rest.ts
  lib/discord-rest.ts:10  new REST({ version: "10" }).setToken(botToken) -- the same computation is written in gateway/register-commands.ts
  lib/interaction-response.ts:47  createJsonResponse({ type: InteractionResponseType.DEFERRED_ -- the same computation is written in src/commands/music.command.ts
  src/commands/dream-journey.command.ts:46  getDiscordRestClient(env.DISCORD_BOT_TOKEN).post( Routes.web -- the same computation is written in src/commands/whistle.command.ts
  src/commands/music.command.ts:347  createJsonResponse({ type: InteractionResponseType.DEFERRED_ -- the same computation is written in lib/interaction-response.ts
  src/commands/whistle.command.ts:49  getDiscordRestClient(env.DISCORD_BOT_TOKEN).post( Routes.web -- the same computation is written in src/commands/dream-journey.command.ts
```

### no-duplicated-function-body

- layer: resilience, tier: prepass, severity: warn
- rule: a function whose body is identical to the same-named function in another file must be lifted to a shared module
- why: an identical copy is not a coincidence, it is the same decision written twice, so a fix or a rule change has to be found and applied in every copy, and the copies drift one bug report at a time because nothing marks them as related, the compiler sees two independent functions and so does every reader
- replacement: move the implementation into the shared library and import it, keeping one declaration whose name is the single answer to what the helper does
- hits: 48 across 23 files (holdout 0)

evidence:

```
  gateway/bridge.smoke.ts:21  assert is a byte-identical copy, 4 copies across 4 files
  gateway/market-price.smoke.ts:19  assert is a byte-identical copy, 4 copies across 4 files
  gateway/member-join-bridge.ts:79  isRecord is a byte-identical copy, 10 copies across 10 files
  gateway/music-bot.smoke.ts:36  assert is a byte-identical copy, 4 copies across 4 files
  gateway/queue.smoke.ts:26  assert is a byte-identical copy, 4 copies across 4 files
  src/commands/afk.command.ts:38  isStringOption is a byte-identical copy, 3 copies across 3 files
  src/commands/buy.command.ts:36  isStringOption is a byte-identical copy, 3 copies across 3 files
  src/commands/family.command.ts:72  isSubcommandOption is a byte-identical copy, 2 copies across 2 files
  src/commands/family.command.ts:77  isUserOption is a byte-identical copy, 6 copies across 6 files
```

### no-duplicated-user-facing-copy

- layer: cosmetic, tier: prepass, severity: warn
- rule: the same user-facing sentence must not be written out in 3 or more files
- why: copy that is pasted into each file drifts the moment one site is edited, the reader cannot tell whether two identical sentences are the same message or two messages that happen to match today, and a wording change becomes a search across the codebase instead of one edit at the owning constant
- replacement: declare the sentence once in the owning surface's .config.ts enum and import it, or lift it to lib when several surfaces share it, keeping the literal only where the duplication is deliberate across process boundaries
- hits: 19 across 19 files (holdout 0)

evidence:

```
  src/commands/afk.command.config.ts:7  "This command only works in a server." also appears in 18 other file(s)
  src/commands/buy.command.config.ts:8  "This command only works in a server." also appears in 18 other file(s)
  src/commands/divination.command.config.ts:4  "This command only works in a server." also appears in 18 other file(s)
  src/commands/dream-fishing.command.config.ts:2  "This command only works in a server." also appears in 18 other file(s)
  src/commands/dreams.command.config.ts:2  "This command only works in a server." also appears in 18 other file(s)
  src/commands/econ.command.config.ts:2  "This command only works in a server." also appears in 18 other file(s)
  src/commands/family.command.config.ts:16  "This command only works in a server." also appears in 18 other file(s)
  src/commands/give.command.config.ts:7  "This command only works in a server." also appears in 18 other file(s)
```

### no-literal-duplicating-config-value

- layer: cosmetic, tier: prepass, severity: warn
- rule: a string literal must not retype a value the surface's own config already declares
- why: the enum is the single source of truth for the value, and the literal is a second copy that the compiler cannot keep in step, changing the enum silently leaves the literal comparing against the old text, and a reader cannot tell whether the match is deliberate or whether it used to be a different value
- replacement: import the enum and reference the declaration, so a rename or a value change is a one-line edit that every comparison follows
- hits: 16 across 2 files (holdout 0)

evidence:

```
  src/commands/dreams.command.ts:47  "dreams" duplicates DreamEmojiConfig.EmojiName
  src/commands/music.command.ts:94  "play" duplicates MusicSubcommandName.Play
  src/commands/music.command.ts:108  "volume" duplicates MusicSubcommandName.Volume
```

### no-renamed-duplicate-body

- layer: resilience, tier: prepass, severity: warn
- rule: a function body copied into another file under a new name is the same implementation
- why: the identity of a helper is its body and not its name, so a copy whose author renamed the declaration is still one decision written twice: a fix has to be found in every copy, the copies drift one bug report at a time, and a search for the helper's name finds only the copy that kept it. the accepted rule for identical bodies keys on the name as well, which is what makes it exact, and that same key is why a renamed copy is invisible to it.
- replacement: keep one declaration in the shared module at the lowest layer that both callers can reach, and give it the name both call sites can read. a caller that narrows the result differently imports the shared predicate instead of restating its body.
- hits: 8 across 7 files (holdout 0)

evidence:

```
  src/services/afk.service.ts:187  isRecordWithId is a byte-identical body, also declared 8 times across 7 files as isMessageWithId, isChannelWithId -- for example src/services/dream-journey.service.ts
  src/services/afk.service.ts:197  isMessageWithId is a byte-identical body, also declared 8 times across 7 files as isRecordWithId, isChannelWithId -- for example src/services/dream-journey.service.ts
  src/services/dream-journey.service.ts:366  isMessageWithId is a byte-identical body, also declared 8 times across 7 files as isRecordWithId, isChannelWithId -- for example src/services/afk.service.ts
  src/services/family-consent.service.ts:171  isMessageWithId is a byte-identical body, also declared 8 times across 7 files as isRecordWithId, isChannelWithId -- for example src/services/afk.service.ts
  src/services/family.service.ts:150  isRecordWithId is a byte-identical body, also declared 8 times across 7 files as isMessageWithId, isChannelWithId -- for example src/services/afk.service.ts
  src/services/pillow-fight.service.ts:306  isMessageWithId is a byte-identical body, also declared 8 times across 7 files as isRecordWithId, isChannelWithId -- for example src/services/afk.service.ts
  src/services/sleep.service.ts:302  isChannelWithId is a byte-identical body, also declared 8 times across 7 files as isRecordWithId, isMessageWithId -- for example src/services/afk.service.ts
  src/services/sleepover.service.ts:216  isMessageWithId is a byte-identical body, also declared 8 times across 7 files as isRecordWithId, isChannelWithId -- for example src/services/afk.service.ts
```

### no-repeated-inline-copy

- layer: cosmetic, tier: prepass, severity: warn
- rule: user-facing copy must not be written out twice inside one file
- why: the cross-file copy rule only sees a sentence once it has spread to three files, so the smallest and most common form of the defect is invisible: two statements in one file carrying the same sentence, where a wording change updates one of them and leaves the other saying something the author no longer means, and neither copy is reachable from the config entry that owns the rest of the surface's copy
- replacement: declare the sentence once, as an entry in the owning .config.ts where the surface already keeps its user-facing text or as a module-level constant otherwise, and let every statement name that entry instead of repeating the sentence
- hits: 15 across 5 files (holdout 0)

evidence:

```
  gateway/music-bot.ts:928  "Nothing is playing right now." written 2 times in this file
  gateway/music-bot.ts:1022  "Nothing is playing right now." written 2 times in this file
  src/bot.ts:69  "Invalid bridge secret." written 2 times in this file
  src/bot.ts:113  "Invalid bridge secret." written 2 times in this file
  src/db/accounts.repo.ts:59  "INSERT INTO accounts (user_id, balance) VALUES (?," written 2 times in this file
  src/db/accounts.repo.ts:215  "INSERT INTO accounts (user_id, balance) VALUES (?," written 2 times in this file
  src/services/family-tree.service.ts:324  "Storage error." written 2 times in this file
  src/services/family-tree.service.ts:336  "Storage error." written 2 times in this file
```

### require-capitalised-user-facing-copy

- layer: cosmetic, tier: grit, severity: warn
- rule: user-facing copy must start with a capital letter
- why: copy that starts lowercase reaches the user as a sentence fragment, the same message renders differently depending on whether it is shown alone or spliced after another line, and a rule the project already documents stops being enforceable the moment it lives only in a style guide
- replacement: capitalise the first letter of the sentence, keeping proper nouns and inline identifiers as they are, and keep template placeholders out of the first position so the capital is not swallowed by substitution
- hits: 38 across 5 files (holdout 0)

evidence:

```
  src/commands/music.command.config.ts:68  "and {hiddenCount} more queued"
  src/services/guide-shelf.service.config.ts:213  "the campfire of the land, where the whole server talks"
  src/services/guide-shelf.service.config.ts:223  "the bot channel, where the golems do their work"
  src/services/liked-shelf.service.config.ts:41  "and {hiddenCount} more in your Liked Music"
  src/services/pillow-fight.service.config.ts:110  "slam in a heavy blow"
  src/services/pillow-fight.service.config.ts:116  "sweep their legs"
  src/services/spirit-chase.service.config.ts:110  "stray spirit"
  src/services/spirit-chase.service.config.ts:147  "legendary spirit"
```

## performance

### no-await-in-loop

- layer: behavioural, tier: grit, severity: warn
- rule: an await inside a loop serialises every iteration
- why: an await in a loop body suspends once per element, so a loop over n items costs n sequential round trips instead of one concurrent batch, the latency is n times the slowest dependency and the pattern silently turns a linear scan into the dominant cost of the request
- replacement: map the items to promises and await Promise.all once, or use Promise.allSettled when one rejection must not cancel the rest, and keep the loop only when iteration n truly depends on iteration n minus one
- hits: 11 across 5 files (holdout 0)

evidence:

```
  src/services/family-consent.service.ts:582  for (const targetId of activeTargetIds) { const record = (await familyKv.get<ConsentRecord>( buildConsentKey(targetId), 
  src/services/family.service.ts:235  for (const key of keyList.keys) { const record = await familyKv.get<FamilyRecord>(key.name, "json"); if (record && recor
  src/services/family.service.ts:296  for (const userId of memberIds) { const addOutcome = await patchMemberMarriedRole( botToken, guildId, marriedRoleId, use
  src/services/pillow-fight.service.ts:933  for (const actorId of activeActorIds) { const record = (await afkKv.get<PillowFightRecord>( buildPillowFightKey(actorId)
  src/services/sleepover.service.ts:793  for (const targetId of activeTargetIds) { const record = (await sleepKv.get<SleepoverRecord>( buildSleepoverKey(targetId
  src/services/spirit-chase.service.ts:1497  for (const chaseRecord of expiredOutcome.result) { const messageDissolved = await dissolveChaseMessage( botToken, chaseR
  src/services/spirit-chase.service.ts:1552  for (const pendingChaseId of pendingCleanupIds) { await cleanupSpiritChaseTrailMessages( afkKv, dreamsDb, botToken, pend
```

## resilience

### no-async-scalar-failure-return

- layer: resilience, tier: prepass, severity: warn
- rule: an async operation that can fail must not report its failure as a bare boolean or number
- why: a scalar result carries no failure channel, so the caller cannot separate the operation's real answer from its error case, the reason the operation already holds is dropped at the boundary that had it, and every caller is forced to guess which way the value went, which is the collapse this codebase's outcome values exist to prevent
- replacement: declare the result as the outcome value carrying both branches, so the settled type names a failure reason beside the data, and a caller that has no use for the outcome can narrow it out explicitly instead of guessing at a scalar
- hits: 5 across 3 files (holdout 0)

evidence:

```
  lib/kv-register.ts:50  addRegisterId(): Promise<boolean> settles to boolean
  lib/kv-register.ts:65  removeRegisterId(): Promise<boolean> settles to boolean
  src/services/dream-fishing.service.ts:100  fetchRodOwnership(): Promise<boolean> settles to boolean
  src/services/xp.service.ts:92  readXpBoostMultiplier(): Promise<number> settles to number
  src/services/xp.service.ts:137  grantLevelRole(): Promise<boolean> settles to boolean
```

### no-discarded-outcome

- layer: behavioural, tier: grit, severity: warn
- rule: a call that returns Outcome must have its succeeded field narrowed
- why: the behavioural layer routes every failure through an Outcome so the caller cannot forget it, but an Outcome used as a bare statement is discarded before it is read, the failure branch becomes unreachable and the error disappears with no log and no return, which is the exact silent failure the Outcome pattern was introduced to prevent
- replacement: assign the result and branch on succeeded, returning a matching failure when the call fails, and where the call is genuinely best effort, say so by logging the failure instead of dropping the value
- hits: 21 across 7 files (holdout 0)

evidence:

```
  src/bot.ts:235  await dissolveExpiredSpiritChases( env.AFK_KV, env.DREAMS_DB, env.DISCORD_BOT_TOKEN, ); -- returns Promise<DissolveSpiritChasesOutcome>
  src/commands/sleep.command.ts:226  await setSleepCooldown(env.AFK_KV, userId, nextReadyAt); -- returns Promise<SetSleepCooldownOutcome>
  src/commands/sleep.command.ts:288  await setSleepCooldown(env.AFK_KV, userId, nextReadyAt); -- returns Promise<SetSleepCooldownOutcome>
  src/services/afk.service.ts:336  await restoreNickname( botToken, recordOutcome.result.guildId, message.author.id, recordOutcome.resu -- returns Promise<RestoreNicknameOutcome>
  src/services/afk.service.ts:356  await postAfkReply( botToken, message.channel_id, buildAfkReplyEmbed(recordOutcome.result), context, -- returns Promise<PostAfkReplyOutcome>
  src/services/pillow-fight.service.ts:600  await creditAccountBalance( env.DREAMS_DB, record.actorId, record.wagerDreams, ); -- returns Promise<UpdateAccountBalanceOutcome>
  src/services/pillow-fight.service.ts:608  await creditAccountBalance( env.DREAMS_DB, record.actorId, record.wagerDreams, ); -- returns Promise<UpdateAccountBalanceOutcome>
  src/services/sleep.service.ts:501  await debitAccountBalanceClamped(dreamsDb, record.userId, stolenDreams); -- returns Promise<UpdateAccountBalanceOutcome>
  src/services/sleep.service.ts:504  await creditAccountBalance( dreamsDb, record.userId, dreamcatcherRefundDreams, ); -- returns Promise<UpdateAccountBalanceOutcome>
```

### no-unread-scalar-result

- layer: behavioural, tier: prepass, severity: warn
- rule: a call whose declared result is a bare boolean or number must have that result read
- why: a function that settles to a scalar can only report failure by returning the value that also means a real answer, so when the caller drops the result the operation has neither reported its failure nor been told to, the failure channel exists only in the signature, and the reader of the call site cannot tell whether the author decided the result did not matter or never noticed there was one
- replacement: read the result and act on it, or where the call is genuinely best effort, log the failure at the call site so the intent is stated, or narrow the callee to a void result so the signature stops claiming a failure the caller never consumes
- hits: 25 across 7 files (holdout 0)

evidence:

```
  src/bot.ts:84  await grantLevelRole( env.DISCORD_BOT_TOKEN, parsedMember.result.guild_id, parsedMember.result.user. -- settles to Promise<boolean>
  src/services/dream-journey.service.ts:548  await editJourneyMessage( context.botToken, context.applicationId, context.interactionToken, context -- settles to Promise<boolean>
  src/services/dream-journey.service.ts:732  await editJourneyMessage( context.botToken, context.applicationId, context.interactionToken, context -- settles to Promise<boolean>
  src/services/family-consent.service.ts:98  await addRegisterId( familyKv, FamilyConsentConfig.ActiveRegisterKey, record.targetId, ); -- settles to Promise<boolean>
  src/services/family-consent.service.ts:118  await removeRegisterId( familyKv, FamilyConsentConfig.ActiveRegisterKey, targetId, ); -- settles to Promise<boolean>
  src/services/pillow-fight.service.ts:136  await addRegisterId( afkKv, PillowFightConfig.ActiveRegisterKey, record.actorId, ); -- settles to Promise<boolean>
  src/services/pillow-fight.service.ts:187  await removeRegisterId( afkKv, PillowFightConfig.ActiveRegisterKey, record.actorId, ); -- settles to Promise<boolean>
  src/services/sleep.service.ts:263  await addRegisterId(sleepKv, SleepConfig.NightmareActiveKey, userId); -- settles to Promise<boolean>
  src/services/sleep.service.ts:596  await removeRegisterId(sleepKv, SleepConfig.NightmareActiveKey, userId); -- settles to Promise<boolean>
```

## sql

### no-duplicated-statement-text

- layer: resilience, tier: prepass, severity: warn
- rule: a statement written twice must be declared once and shared by both call sites
- why: a statement is the contract a module keeps with the shape of its rows, so two copies are two contracts that drift apart: a column added to one and not the other leaves two spellings of the same fact, and the failure is silent because both copies run. a shared receiver hides the repeat from every other duplication check, since one call site prepares it on a handle named for one table and the other prepares it on a handle named for another, and the statement text is the only part that must not be written twice.
- replacement: declare the statement once as a module level constant, or as one exported helper both call sites call, so the text has one home and a schema change is one edit.
- hits: 6 across 2 files (holdout 0)

evidence:

```
  src/db/accounts.repo.ts:59  written 2 times across 1 files: INSERT INTO accounts (user_id, balance) VALUES (?, ?) ON CONFLICT(user
  src/db/accounts.repo.ts:127  written 2 times across 2 files: UPDATE accounts SET balance = balance - ? WHERE user_id = ? AND balanc
  src/db/consumables.repo.ts:47  written 2 times across 2 files: UPDATE accounts SET balance = balance - ? WHERE user_id = ? AND balanc
  src/db/consumables.repo.ts:80  written 2 times across 1 files: INSERT INTO consumables (user_id, item_id, quantity) VALUES (?, ?, 1) 
```

### require-limit-on-collection-reads

- layer: resilience, tier: grit, severity: warn
- rule: a query that reads a collection must carry a LIMIT
- why: a prepare chain that ends in all returns every matching row, a where clause bounds the result only by today's data, one prolific account turns a lookup into a full table transfer, and the cost grows silently because the query text never changes when the row count does
- replacement: add an explicit LIMIT to the select and paginate with an offset or a keyset cursor when the caller genuinely needs everything
- hits: 7 across 5 files (holdout 0)

evidence:

```
  src/db/catches.repo.ts:130  "SELECT user_id FROM catches WHERE fish_id = ? AND quantity > 0"
  src/db/catches.repo.ts:151  "SELECT fish_id, quantity FROM catches WHERE user_id = ? AND quantity > 0 ORDER BY fish_id"
  src/db/consumables.repo.ts:157  "SELECT item_id, quantity FROM consumables WHERE user_id = ? AND quantity > 0 ORDER BY item_id"
  src/db/liked-tracks.repo.ts:94  `SELECT video_id, title, webpage_url, thumbnail_url FROM liked_tracks WHERE user_id = ? ORDER BY liked_at DESC, video_id
  src/db/shop.repo.ts:190  "SELECT item_id FROM shop_purchases WHERE user_id = ? ORDER BY purchased_at"
  src/db/spirit-chase.repo.ts:335  `${spiritChaseSelect} WHERE catcher_user_id IS NULL AND expires_at <= ?`
  src/db/spirit-chase.repo.ts:442  "SELECT channel_id, message_id FROM spirit_chase_messages WHERE chase_id = ?"
```

## types

### no-optional-properties

- layer: resilience, tier: grit, severity: warn
- rule: an object type must not declare an optional property
- why: an optional property splits one type into two states the compiler cannot tell apart, it multiplies the combinations a consumer must guard at every read, and the absence is not part of the type's name so a reader cannot see which subset is valid together, the field is usually required in some flows and missing in others and the difference is a real domain distinction
- replacement: make the property required and default it at the boundary, or model the states as a discriminated union so each variant carries exactly the fields it has, reserve optionality for third-party payload shapes you do not control
- hits: 28 across 5 files (holdout 0)

evidence:

```
  gateway/member-join-bridge.ts:48  readonly t?: string;
  gateway/member-join-bridge.ts:49  readonly d?: unknown;
  gateway/music-bot.ts:425  readonly embeds?: readonly APIEmbed[];
  gateway/music-bot.ts:426  readonly components?: readonly APIActionRowComponent<APIButtonComponent>[];
  gateway/yt-dlp-exec.types.d.ts:17  readonly id?: string;
  gateway/yt-dlp-exec.types.d.ts:18  readonly title?: string;
  src/services/pillow-fight.types.ts:24  readonly targetMove?: PillowFightMove;
  src/services/pillow-fight.types.ts:25  readonly actorMove?: PillowFightMove;
```

### require-enum-over-literal-union

- layer: resilience, tier: grit, severity: warn
- rule: a union of two or more string literals must be an enum
- why: a union of string literals is the weaker half of the construct the shipped as-const ban exists to close. it carries the type but no runtime value, so every comparison against it is a retyped magic string, the set of allowed values is invisible at the call site, and adding a member is a text edit nobody can search for. the enum the project mandates for the object form gives both the type and one place to name the value, and the two forms are used for the same decision: a string that selects a behaviour
- replacement: declare a string enum beside the config it belongs to and use its members as the discriminant, so the allowed values exist once as a value and once as a type. the union form is only correct where the value must survive a boundary that cannot execute an enum, which is why process entry modules are out of scope here
- hits: 3 across 3 files (holdout 0)

evidence:

```
  src/commands/music.command.ts:115  "promote" | "remove"
  src/services/music.service.ts:92  "up" | "down"
  src/services/pillow-fight.service.ts:74  "actor" | "target" | "tie"
```

### require-readonly-collection-signatures

- layer: resilience, tier: grit, severity: warn
- rule: an array in a function signature or object type must be declared readonly
- why: a parameter typed as a mutable array hands the callee the caller's array and the caller's permission to reorder it, nothing in the signature says whether the function only reads, the mutation then travels back through an alias and surfaces far from the call, on the return side a mutable array claims the caller owns the producer's buffer and may edit it, and on an object type the property modifier only stops reassignment while leaving push and sort free to run on shared data
- replacement: type the parameter, the return and the object-type property as readonly T[] or ReadonlyArray<T>, widening a downstream API signature when it rejects readonly input rather than dropping the guarantee. class fields are deliberately out of scope, a mutable field is state the class owns rather than a value it was handed
- hits: 30 across 12 files (holdout 0)

evidence:

```
  gateway/music-bot.ts:613  string[]
  gateway/music-messages.ts:194  APIActionRowComponent<APIButtonComponentWithCustomId>[]
  src/services/divination.service.ts:250  string[]
  src/services/family-consent.service.ts:354  components: APIMessageTopLevelComponent[]
  src/services/family-consent.service.ts:538  components: APIMessageTopLevelComponent[]
  src/services/guide-shelf.service.ts:256  APIEmbedField[]
  src/services/guide-shelf.service.ts:428  APIEmbedField[]
  src/services/liked-shelf.service.ts:111  LikedButtonRow[]
```

### require-readonly-type-members

- layer: resilience, tier: grit, severity: warn
- rule: every property of an object type must be readonly
- why: a mutable property on a shared object type lets any consumer mutate a value another layer still holds, which makes the mutation site invisible and the data flow untraceable, the type stops being a description of shape and becomes a mutable cell
- replacement: add the readonly modifier to every property signature, and when a layer genuinely needs to produce a changed copy, build a new object of the same type rather than editing one in place
- hits: 13 across 5 files (holdout 0)

evidence:

```
  gateway/member-join-bridge.ts:131  heartbeatTimer: ReturnType<typeof setInterval> | undefined;
  gateway/member-join-bridge.ts:132  heartbeatIntervalMs: number;
  gateway/music-bot.smoke.ts:53  decodedByteCount: number;
  gateway/music-bot.smoke.ts:53  elapsedMs: number
  src/commands/guide.command.ts:24  type: ApplicationCommandOptionType.String
  src/commands/music.command.ts:49  type: ApplicationCommandOptionType.String
  src/commands/music.command.ts:56  type: ApplicationCommandOptionType.Integer
  src/services/sleep.service.ts:302  id: string
```

## rejected

- max-nesting-depth-three: too_few_files:1, too_few_hits:2
- no-mutating-array-methods: too_few_files:1
- no-shadowed-binding: too_few_files:0, too_few_hits:0
- no-unused-exports: too_few_files:1, too_few_hits:1

