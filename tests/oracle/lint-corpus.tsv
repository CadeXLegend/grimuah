src/components:0: [structural] surface 'components' names the directory src/components/, which does not exist; create it or drop the surface from architecture.config.json
src/probe/any-type.ts:5: [resilience] This `any` type bypasses type safety. Write the type you mean instead.
src/probe/any-type.ts:5: [resilience] This `any` type bypasses type safety. Write the type you mean instead.
src/probe/any-type.ts:5: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/any-type.ts:5: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/any-type.ts:9: [resilience] This `any` type bypasses type safety. Write the type you mean instead.
src/probe/asany-arr.ts:3: [resilience] 'as any' bypasses type safety entirely; use a proper type instead
src/probe/asany-arr.ts:3: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/asany-param.ts:1: [resilience] This `any` type bypasses type safety. Write the type you mean instead.
src/probe/asany-param.ts:1: [resilience] This `any` type bypasses type safety. Write the type you mean instead.
src/probe/asany.ts:1: [resilience] 'as any' bypasses type safety entirely; use a proper type instead
src/probe/asany-type.ts:1: [resilience] This `any` type bypasses type safety. Write the type you mean instead.
src/probe/asany-type.ts:2: [resilience] This `any` type bypasses type safety. Write the type you mean instead.
src/probe/asany-union.ts:1: [resilience] 'as any' bypasses type safety entirely; use a proper type instead
src/probe/asany-union.ts:1: [resilience] This `any` type bypasses type safety. Write the type you mean instead.
src/probe/asconst-local.ts:1: [resilience] use enum instead of const + as const; enum gives you both value and type in one declaration
src/probe/asconst.ts:1: [resilience] use enum instead of const + as const; enum gives you both value and type in one declaration
src/probe/await-in-loop.ts:11: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/await-in-loop.ts:17: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/await-in-loop.ts:26: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/await-in-loop.ts:27: [behavioural] This loop awaits inside its body, so every iteration runs in sequence. Map the items to promises and await Promise.all once.
src/probe/await-in-loop.ts:5: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/await-in-loop.ts:6: [behavioural] This loop awaits inside its body, so every iteration runs in sequence. Map the items to promises and await Promise.all once.
src/probe/barecatch-comment.ts:4: [behavioural] catch block must handle or log the error, not silently discard it
src/probe/barecatch-comment.ts:4: [behavioural] do not use bare catch with silent failure; log the error or return an Outcome
src/probe/barecatch.ts:4: [behavioural] catch block must handle or log the error, not silently discard it
src/probe/barecatch.ts:4: [behavioural] do not use bare catch with silent failure; log the error or return an Outcome
src/probe/barecatch-ws.ts:4: [behavioural] catch block must handle or log the error, not silently discard it
src/probe/barecatch-ws.ts:4: [behavioural] do not use bare catch with silent failure; log the error or return an Outcome
src/probe/boolean-flag-argument.ts:5: [resilience] This call passes a bare boolean literal. Name the behaviour instead, or pass a named enum value.
src/probe/boolean-flag-argument.ts:7: [resilience] This call passes a bare boolean literal. Name the behaviour instead, or pass a named enum value.
src/probe/c-both-catch.ts:4: [behavioural] catch block must handle or log the error, not silently discard it
src/probe/c-both-catch.ts:4: [hygiene] This variable e is unused.
src/probe/c-comment-only-bound.ts:4: [behavioural] catch block must handle or log the error, not silently discard it
src/probe/c-comment-only-bound.ts:4: [hygiene] This variable e is unused.
src/probe/chained-paren.ts:1: [resilience] chained 'as' casts bypass type safety; use a single cast only
src/probe/chained.ts:1: [resilience] chained 'as' casts bypass type safety; use a single cast only
src/probe/c-nested-empty.ts:4: [behavioural] catch block must handle or log the error, not silently discard it
src/probe/c-nested-empty.ts:7: [behavioural] catch block must handle or log the error, not silently discard it
src/probe/c-nested-empty.ts:7: [behavioural] do not use bare catch with silent failure; log the error or return an Outcome
src/probe/config-declares-data-only.config.ts:12: [structural] This .config.ts file declares a function. Move the behaviour into the surface's own module.
src/probe/config-declares-data-only.config.ts:16: [structural] This .config.ts file declares a function. Move the behaviour into the surface's own module.
src/probe/c-throw-noval.ts:5: [behavioural] do not use throw; all errors must flow through OperationOutcome. see lib/outcome.ts
src/probe/cyclomatic-complexity.ts:5: [resilience] This function has a cyclomatic complexity of 18. Extract each decision into a named predicate or a lookup.
src/probe/d-as-generic.ts:2: [resilience] 'as any' bypasses type safety entirely; use a proper type instead
src/probe/d-as-generic.ts:2: [resilience] chained 'as' casts bypass type safety; use a single cast only
src/probe/d-as-newline.ts:2: [hygiene] This variable b is unused.
src/probe/d-as-newline.ts:3: [resilience] chained 'as' casts bypass type safety; use a single cast only
src/probe/d-as-object-type.ts:1: [resilience] chained 'as' casts bypass type safety; use a single cast only
src/probe/d-as-object-type.ts:1: [resilience] This property is mutable. Add `readonly`, and build a new object when a layer needs a changed copy.
src/probe/d-as-union.ts:1: [resilience] 'as any' bypasses type safety entirely; use a proper type instead
src/probe/d-destructure-asconst.ts:1: [resilience] This property is mutable. Add `readonly`, and build a new object when a layer needs a changed copy.
src/probe/d-destructure-asconst.ts:2: [resilience] use enum instead of const + as const; enum gives you both value and type in one declaration
src/probe/d-export-brace-multiline.ts:1: [resilience] do not proxy re-export; every export must originate from the file that defines it
src/probe/d-export-brace-nested.ts:1: [resilience] do not proxy re-export; every export must originate from the file that defines it
src/probe/d-foo-qualified.ts:1: [resilience] chained 'as' casts bypass type safety; use a single cast only
src/probe/d-for-nested.ts:1: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/d-for-nested.ts:2: [resilience] do not use let; use const. only let at module-level mutable caches
src/probe/d-for-nested.ts:3: [resilience] do not use imperative for loops; use map, filter, reduce, or for..of instead
src/probe/d-for-nested.ts:3: [resilience] do not use let; use const. only let at module-level mutable caches
src/probe/d-for-parens.ts:2: [resilience] do not use imperative for loops; use map, filter, reduce, or for..of instead
src/probe/d-for-parens.ts:3: [resilience] chained 'as' casts bypass type safety; use a single cast only
src/probe/d-for-parens.ts:3: [resilience] This property is mutable. Add `readonly`, and build a new object when a layer needs a changed copy.
src/probe/d-for-parens.ts:4: [resilience] chained 'as' casts bypass type safety; use a single cast only
src/probe/d-for-parens.ts:4: [resilience] This property is mutable. Add `readonly`, and build a new object when a layer needs a changed copy.
src/probe/d-for-parens.ts:5: [resilience] chained 'as' casts bypass type safety; use a single cast only
src/probe/d-for-parens.ts:5: [resilience] This property is mutable. Add `readonly`, and build a new object when a layer needs a changed copy.
src/probe/d-multiline-asconst.ts:1: [resilience] use enum instead of const + as const; enum gives you both value and type in one declaration
src/probe/d-null-arrow.ts:1: [resilience] do not use null; use undefined. null only at third-party boundaries (DB, RegExp)
src/probe/d-null-objectkey.ts:1: [resilience] do not use null; use undefined. null only at third-party boundaries (DB, RegExp)
src/probe/d-null-objectkey.ts:1: [resilience] do not use null; use undefined. null only at third-party boundaries (DB, RegExp)
src/probe/d-null-objectkey.ts:2: [resilience] do not use null; use undefined. null only at third-party boundaries (DB, RegExp)
src/probe/d-throw-cond.ts:2: [behavioural] do not use throw; all errors must flow through OperationOutcome. see lib/outcome.ts
src/probe/d-two-as.ts:1: [resilience] chained 'as' casts bypass type safety; use a single cast only
src/probe/emdash-comment.ts:1: [cosmetic] do not use em-dashes; use commas, colons, or sentence breaks instead
src/probe/emdash-string.ts:1: [cosmetic] do not use em-dashes; use commas, colons, or sentence breaks instead
src/probe/emdash-template.ts:1: [cosmetic] do not use em-dashes; use commas, colons, or sentence breaks instead
src/probe/eq-double.ts:1: [resilience] use === instead of == to avoid type coercion bugs
src/probe/e-return-in-string.ts:4: [behavioural] catch block must handle or log the error, not silently discard it
src/probe/for-cstyle.ts:2: [resilience] do not use let; use const. only let at module-level mutable caches
src/probe/for-cstyle.ts:3: [resilience] do not use imperative for loops; use map, filter, reduce, or for..of instead
src/probe/for-cstyle.ts:3: [resilience] do not use let; use const. only let at module-level mutable caches
src/probe/for-empty-init.ts:2: [resilience] do not use imperative for loops; use map, filter, reduce, or for..of instead
src/probe/for-global.ts:2: [resilience] do not use imperative for loops; use map, filter, reduce, or for..of instead
src/probe/for-in.ts:2: [resilience] do not use let; use const. only let at module-level mutable caches
src/probe/for-of-accumulation.ts:14: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/for-of-accumulation.ts:25: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/for-of-accumulation.ts:5: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/for-of-accumulation.ts:7: [resilience] This for..of loop builds an array by pushing into it. Use map, filter, flatMap or reduce instead.
src/probe/for-of.ts:1: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/for-of.ts:2: [resilience] do not use let; use const. only let at module-level mutable caches
src/probe/handled-comment.ts:4: [behavioural] catch block must handle or log the error, not silently discard it
src/probe/handled-comment.ts:4: [behavioural] do not use bare catch with silent failure; log the error or return an Outcome
src/probe/handled-throw.ts:5: [behavioural] do not use throw; all errors must flow through OperationOutcome. see lib/outcome.ts
src/probe/if-chain-dispatch.ts:6: [resilience] These 3 branches dispatch on one subject. Declare a Record or Map from the subject's value to the handler.
src/probe/let-destructure.ts:1: [resilience] This property is mutable. Add `readonly`, and build a new object when a layer needs a changed copy.
src/probe/let-destructure.ts:2: [hygiene] This let declares a variable that is only assigned once.
src/probe/let-destructure.ts:2: [resilience] do not use let; use const. only let at module-level mutable caches
src/probe/let-init.ts:2: [hygiene] This let declares a variable that is only assigned once.
src/probe/let-init.ts:2: [resilience] do not use let; use const. only let at module-level mutable caches
src/probe/let-noinit.ts:2: [hygiene] This let declares a variable that is only assigned once.
src/probe/let-noinit.ts:2: [resilience] do not use let; use const. only let at module-level mutable caches
src/probe/let-typed.ts:2: [hygiene] This let declares a variable that is only assigned once.
src/probe/let-typed.ts:2: [resilience] do not use let; use const. only let at module-level mutable caches
src/probe/let-var.ts:2: [resilience] do not use let; use const. only let at module-level mutable caches
src/probe/lowercase-copy.config.ts:14: [cosmetic] This copy starts lowercase. Capitalise the first letter of the sentence.
src/probe/lowercase-copy.config.ts:6: [cosmetic] This copy starts lowercase. Capitalise the first letter of the sentence.
src/probe/max-file-lines.ts:1: [resilience] This file is 501 lines long. Split it along the responsibilities its sections already show.
src/probe/max-function-lines.ts:5: [resilience] This function body is 81 lines long. Extract each distinct job into a named function.
src/probe/max-parameters.ts:5: [resilience] This function takes 5 parameters. Group them into a named readonly type, or split the function.
src/probe/nested-ternary.ts:10: [resilience] This conditional expression contains another conditional expression. Extract the inner decision into a named helper or a lookup.
src/probe/nested-ternary.ts:6: [resilience] This conditional expression contains another conditional expression. Extract the inner decision into a named helper or a lookup.
src/probe/nesting-depth.ts:19: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/nesting-depth.ts:3: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/nesting-depth.ts:7: [resilience] This block is nested 4 levels deep. Return early or extract a helper.
src/probe/nesting-depth.ts:8: [resilience] This block is nested 5 levels deep. Return early or extract a helper.
src/probe/no-async-scalar-failure-return.ts:11: [resilience] This async operation reports its failure as a bare boolean or number, so a caller cannot tell the answer from the error. Return an outcome value that names the failure reason.
src/probe/no-async-scalar-failure-return.ts:15: [resilience] This async operation reports its failure as a bare boolean or number, so a caller cannot tell the answer from the error. Return an outcome value that names the failure reason.
src/probe/no-async-scalar-failure-return.ts:17: [resilience] This async operation reports its failure as a bare boolean or number, so a caller cannot tell the answer from the error. Return an outcome value that names the failure reason.
src/probe/no-optional-properties.ts:11: [resilience] This property is optional. Make it required and default it at the boundary, or model the states as a discriminated union.
src/probe/no-optional-properties.ts:16: [resilience] This property is optional. Make it required and default it at the boundary, or model the states as a discriminated union.
src/probe/no-optional-properties.ts:17: [resilience] This property is optional. Make it required and default it at the boundary, or model the states as a discriminated union.
src/probe/no-optional-properties.ts:25: [resilience] This property is optional. Make it required and default it at the boundary, or model the states as a discriminated union.
src/probe/no-optional-properties.ts:30: [resilience] This property is optional. Make it required and default it at the boundary, or model the states as a discriminated union.
src/probe/null-lit.ts:1: [resilience] do not use null; use undefined. null only at third-party boundaries (DB, RegExp)
src/probe/null-nested.ts:1: [resilience] do not use null; use undefined. null only at third-party boundaries (DB, RegExp)
src/probe/null-nested.ts:1: [resilience] do not use null; use undefined. null only at third-party boundaries (DB, RegExp)
src/probe/null-prop.ts:1: [resilience] do not use null; use undefined. null only at third-party boundaries (DB, RegExp)
src/probe/null-prop.ts:2: [resilience] do not use null; use undefined. null only at third-party boundaries (DB, RegExp)
src/probe/null-type.ts:1: [resilience] do not use null; use undefined. null only at third-party boundaries (DB, RegExp)
src/probe/null-type.ts:1: [resilience] This property is mutable. Add `readonly`, and build a new object when a layer needs a changed copy.
src/probe/readonly-collection-signatures.ts:11: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/readonly-collection-signatures.ts:12: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/readonly-collection-signatures.ts:20: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/readonly-collection-signatures.ts:23: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/readonly-collection-signatures.ts:23: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/readonly-collection-signatures.ts:27: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/readonly-collection-signatures.ts:27: [resilience] This signature hands over a mutable array. Declare it as `readonly T[]` or `ReadonlyArray<T>`.
src/probe/readonly-type-members.ts:10: [resilience] This property is mutable. Add `readonly`, and build a new object when a layer needs a changed copy.
src/probe/readonly-type-members.ts:12: [resilience] This property is mutable. Add `readonly`, and build a new object when a layer needs a changed copy.
src/probe/reexport-empty.ts:1: [resilience] do not proxy re-export; every export must originate from the file that defines it
src/probe/reexport-renamed.ts:1: [resilience] do not proxy re-export; every export must originate from the file that defines it
src/probe/reexport-star-namespace.ts:1: [resilience] do not proxy re-export; every export must originate from the file that defines it
src/probe/reexport-star.ts:3: [resilience] do not proxy re-export; every export must originate from the file that defines it
src/probe/reexport.ts:1: [resilience] do not proxy re-export; every export must originate from the file that defines it
src/probe/require-enum-over-literal-union.service.ts:13: [resilience] This union of 2 string literals carries no runtime value. Declare a string enum and use its members as the discriminant.
src/probe/require-enum-over-literal-union.service.ts:17: [resilience] This union of 2 string literals carries no runtime value. Declare a string enum and use its members as the discriminant.
src/probe/require-enum-over-literal-union.service.ts:20: [resilience] This union of 2 string literals carries no runtime value. Declare a string enum and use its members as the discriminant.
src/probe/require-enum-over-literal-union.service.ts:22: [resilience] This union of 2 string literals carries no runtime value. Declare a string enum and use its members as the discriminant.
src/probe/require-enum-over-literal-union.service.ts:24: [resilience] This union of 2 string literals carries no runtime value. Declare a string enum and use its members as the discriminant.
src/probe/sheet.js:1: [resilience] use === instead of == to avoid type coercion bugs
src/probe/silent-catch-bound.ts:4: [behavioural] catch block must handle or log the error, not silently discard it
src/probe/silent-catch.ts:4: [behavioural] catch block must handle or log the error, not silently discard it
src/probe/switch-basic.ts:2: [resilience] do not use switch; use a dispatch table (Record/Map) instead
src/probe/switch-stmt.ts:2: [resilience] do not use switch; use a dispatch table (Record/Map) instead
src/probe/throw-stmt.ts:2: [behavioural] do not use throw; all errors must flow through OperationOutcome. see lib/outcome.ts
src/probe/unbounded-collection-read.ts:6: [resilience] This query reads a collection with no LIMIT, so it returns every matching row. Add an explicit LIMIT and paginate when the caller needs everything.
src/probe/x-catch-destructure.ts:4: [behavioural] catch block must handle or log the error, not silently discard it
src/probe/y-dead-only.ts:11: [cosmetic] do not use em-dashes; use commas, colons, or sentence breaks instead
src/probe/y-dead-only.ts:12: [cosmetic] do not use em-dashes; use commas, colons, or sentence breaks instead
src/probe/y-dead-only.ts:2: [resilience] do not use let; use const. only let at module-level mutable caches
src/probe/y-dead-only.ts:3: [resilience] do not use switch; use a dispatch table (Record/Map) instead
src/probe/y-reexport-comment.ts:1: [resilience] do not proxy re-export; every export must originate from the file that defines it
src/probe/y-reexport-line-comment.ts:1: [resilience] do not proxy re-export; every export must originate from the file that defines it
src/probe/y-reexport-newline.ts:1: [resilience] do not proxy re-export; every export must originate from the file that defines it
src/probe/z-jsx-text.tsx:4: [resilience] do not use null; use undefined. null only at third-party boundaries (DB, RegExp)
src/probe/z-jsx-text.tsx:5: [resilience] 'as any' bypasses type safety entirely; use a proper type instead
src/services:0: [structural] surface 'services' names the directory src/services/, which does not exist; create it or drop the surface from architecture.config.json
src/utils:0: [structural] surface 'utils' names the directory src/utils/, which does not exist; create it or drop the surface from architecture.config.json
