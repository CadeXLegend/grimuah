// the third file that writes the sentence, which is what makes the group a defect. the
// sentence the other two files share is deliberately absent here, so it stays at two
// files and reports nothing

export const NothingPlaying = "Nothing is playing right now.";
