// the second of the three files that write this sentence. the file counts once however
// often it writes the sentence, which is why `NothingPlaying` appears here and in the
// first file only once each

export const NothingPlaying = "Nothing is playing right now.";

export const OnlyTwoFiles = "Only two files write this one.";
