// a body past the length cap: the statements below run 81 lines between the
// braces, one past the limit. the corpus is linted with every layer on, so
// nothing here is a banned construct

export function longBody(seed: number): number {
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  seed = seed + 1;
  return seed;
}
